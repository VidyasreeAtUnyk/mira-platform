# Postgres connection string for the shared schema (packages/shared-db).
# This app is read-only: it never applies schema.sql itself (see src/lib/db.ts)
# and never writes to these tables. Point this at the same database
# apps/crm/apps/lead-agent use for the "shared" part of "shared schema" to be
# real; falls back to a local no-password dev database if unset (not a
# production connection string -- see packages/shared-db/README.md).
DATABASE_URL=
