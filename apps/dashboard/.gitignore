# dependencies
/node_modules

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files -- .env.example is intentionally NOT ignored (see below): it's
# documentation with no real values, referenced by name from src/app/error.tsx.
# Diverges from apps/crm's blanket `.env*` pattern on purpose, matching
# apps/lead-agent's convention instead -- CLAUDE.md says no credentials get
# committed, not that the example file documenting which env vars exist can't be.
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
