# Postgres connection string for the shared schema (packages/shared-db) plus
# this app's own additive migration (packages/shared-db/migrations). Point
# this at the same database apps/crm's Supabase project / apps/lead-agent
# use for the "shared" part of "shared schema" to be real; falls back to a
# local dev database if unset (see packages/shared-db/README.md and
# PROGRESS-pipeline.md). No credentials are hardcoded anywhere in this app --
# see CLAUDE.md.
DATABASE_URL=
